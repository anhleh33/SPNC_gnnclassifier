name: Playwright Tests
on:
  push:
    branches: [ main, test ]
  pull_request:
    branches: [ main, test ]

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    # --- 🐍 BACKEND SETUP (GLOBAL) ---
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11' 

    # 👇 SIMPLIFIED: Install to the global runner environment
    # Do NOT create a .venv folder here. CI is ephemeral (temporary) anyway.
    - name: Install Backend Dependencies
      run: pip install -r backend/Requirements.txt

    # --- ⚛️ FRONTEND SETUP ---
    - name: Set up Node.js
      uses: actions/setup-node@v4
      with:
        node-version: lts/*

    - name: Install Frontend Dependencies
      run: npm ci

    - name: Install Playwright Browsers
      run: npx playwright install --with-deps

    # --- 🧪 RUN TESTS ---
    - name: Run Playwright tests
      run: npx playwright test
      env:
        DATABASE_URL: postgresql+psycopg://postgres.kmgxlpoiuohradgrwwlm:HCMUE2025%40SPNC@aws-1-ap-northeast-2.pooler.supabase.com:6543/postgres
        PYTHONPATH: ${{ github.workspace }}
        CI: true

    - uses: actions/upload-artifact@v4
      if: ${{ !cancelled() }}
      with:
        name: playwright-report
        path: playwright-report/
        retention-days: 30